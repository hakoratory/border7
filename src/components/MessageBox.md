# MessageBoxコンポーネント

## 入力（引数）
* message {string} 出力するメッセージ
## 出力（戻り値）
JSX.Element

## 設計書
1. Typography  
    1.1 `children` に `message` を渡す

## Test Command
```shell
npm run test components/MessageBox.test.jsx
```
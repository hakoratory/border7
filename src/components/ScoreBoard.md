# ScoreBoardコンポーネント

## 入力（引数）
* score {string} 出力するスコア
## 出力（戻り値）
JSX.Element

## 設計書
1. Typography  
    1.1 `children` に `score` を渡す

## Test Command
```shell
npm run test components/ScoreBoard.test.jsx
```
# Titleコンポーネント

## 入力（引数）
なし
## 出力（戻り値）
JSX.Element

## 設計書
1. Typography  
    1.1 `children` に `'Border7'` を渡す  
2. Typography  
    2.1 `children` に `'with プロプラ!'` を渡す  

### HINT
* `children` に渡す文字列は `src/constants/text.js` にまとめて定義しています。

## Test Command
```shell
npm run test components/Title.test.jsx
```
import { useEffect } from 'react'

export const useKeyDownListener = () => {
  useEffect(() => {
    function click() {
    }

    // addEventListener

    return () => {
      // removeEventListener
    }
  }, [])
}

import { useEffect } from 'react'

export const useKeyPressListener = () => {
  useEffect(() => {
    function click() {
    }

    // addEventListener

    return () => {
      // removeEventListener
    }
  }, [])
}
